/* eslint-disable no-undef */
describe('Pipeline Unit integration test', () => {

  /**
   * TODO: Use Supertest!!!!!
   */

  beforeAll(async() => {
    // setup
  });

  test('Successful POST /pipeline response', () => {
    const response = true;
    expect(response).toBe(true);
  });
});
