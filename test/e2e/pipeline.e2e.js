import request from 'supertest';

/* eslint-disable no-undef */
describe('Pipeline Unit e2e test', () => {

  /**
   * TODO: Use Supertest!!!!!
   */

  beforeAll(async() => {
    // setup
  });

  test('Successful POST /pipeline response', () => {
    const response = true;
    expect(response).toBe(true);
  });
});
